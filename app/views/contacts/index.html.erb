<script src="http://maps.googleapis.com/maps/api/js"></script>
<script>
function initialize() {
  var mapProp = {
    <% @contacts.each do |contact| %>
    center:new google.maps.LatLng(<%= contact.lat %>,<%= contact.lng %>),
    <%end%>
    zoom:16,
    mapTypeId:google.maps.MapTypeId.ROADMAP
  };
  var map=new google.maps.Map(document.getElementById("googleMap"),mapProp);
  var marker = new google.maps.Marker({
    <% @contacts.each do |contact| %>
    position: new google.maps.LatLng(<%= contact.lat %>,<%= contact.lng %>)
    <%end%>,
    title:"Retroware Store"
  });

  // To add the marker to the map, call setMap();
  marker.setMap(map); 
}
google.maps.event.addDomListener(window, 'load', initialize);
</script>

<div class="no-padding contact-intro">
  <div class="w100 map">
    <div id="googleMap" style="width:100%;height:370px; padding: 0px; margin: 0px"></div>
  </div>
</div><!--/.contact-intro || map end-->


  <div>
    <div class="row innerPage">
      <div class="col-lg-12 col-md-12 col-sm-12">
        
        <div class="row userInfo">
          
          <div class="col-xs-12 col-sm-12">
            <% @contacts.each do |contact| %>
            <h1 class="title-big text-center section-title-style2">
              <span> Contact us </span>
            </h1>
            
            <p class="lead text-center">
              <%= contact.content %>
            </p>
            
            <hr>
            
            <div class="row">
            
              <div class="col-lg-6 col-md-6 col-sm-6 col-xs-12">
                
                <h3 class="block-title-5">
                  Customer care 
                </h3>
                
                <p>
                  <%= contact.content %>
                  
                  <br>
                  <strong>
                    Phone number
                  </strong>
                  : 	<%= contact.phone %>
                  <br>
                  <strong>
                    Email us
                  </strong>
                  : 	<%= contact.email %>
                </p>

              <%end%>
              </div>
              <hr>
              <div style="clear:both"> </div>
              
              </div>
              
              
            </div><!--/.row-->
   		</div>
        </div> <!--/.row  ||  -->
        
      </div>
    </div>  <!--/row || innerPage end-->
    <div style="clear:both"> </div>

